export enum AppRoute {
  Main = '/',
  About = '/about',
  AddText = '/addText',
  NotFound = '*'
}

export enum DropdownType {
  Genres = 'genres',
  Authors = 'authors',
  Default = ''
}

export enum RenderStyle {
  Cards = 'cards',
  Table = 'table'
}
